# تحديث طلاب AEC 2.0

ارفع جميع الملفات والمجلدات الموجودة هنا إلى جذر مستودع `Masari-RAED` ثم اضغط **Commit changes**.

لا ترفع المجلد الخارجي الذي يحتوي المشروع؛ المطلوب أن يظهر في جذر GitHub:

- `RaedStudent`
- `RaedStudent.xcodeproj`
- `.github`
- `project.yml`
- `README_AR.md`

بعد الرفع افتح:

`Actions → Build TrollStore IPA - iOS 14.3 → Run workflow`

عند ظهور العلامة الخضراء حمّل Artifact باسم:

`AECStudents-TrollStore-iOS14.3`
